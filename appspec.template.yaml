version: 1
resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "weplate-task"
        LoadBalancerInfo:
          ContainerName: nginx
          ContainerPort: 80